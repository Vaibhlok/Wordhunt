{
  "name": "wordhunt",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@admob-plus/capacitor": "^2.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  },
  "scripts": {
    "build": "echo 'No build needed - using single index.html' && mkdir -p dist && cp index.html dist/index.html"
  }
}
