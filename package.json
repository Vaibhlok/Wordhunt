{
  "name": "word-hunt",
  "version": "1.0.0",
  "scripts": {
    "build": "vite build",
    "cap:sync": "npm run build && npx cap sync"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "@capacitor/android": "^7.0.0",
    "vite": "^6.0.0"
  }
}
